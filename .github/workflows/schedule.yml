name: The Crone Scheduler

on:
  schedule:
    - cron: '*/1 * * * *'

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "Hellow their the time is up its ==> $(date)"
